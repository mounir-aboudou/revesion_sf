<form method="post" action="{{ path('revision_delete', {'id': revision.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ revision.id) }}">
    <button class="btn">Delete</button>
</form>
